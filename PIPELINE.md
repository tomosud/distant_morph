# Processing Pipeline

1. 入力をファイル名の自然順でソートし、PSDは合成画像へ展開する。
2. 黒背景を除外し、許容差単位でRGBを量子化して主要色を検出する。
3. 色ごとに二値マスクを作り、最大の連結輪郭を境界エッジから抽出する。
4. 輪郭周長に沿って128〜512点へ再サンプリングし、隣接キー間で開始点を整列する。
5. 対応点を指定カーブで補間し、色ごとの閉じたSVGパスを生成する。
6. プレビューはSVGをCanvasへ描画する。書き出し時は各SVGをOriginalまたは指定された長辺1024 / 512 / 256pxへ、縦横比を維持してラスター化し、ZIPに格納する。
7. ラスター化したRGB各チャンネルを二値マスク化し、線形時間のユークリッド距離変換で内外の符号付き距離を求める。輪郭を0.5、距離幅を画像長辺の25%として正規化し、追加のRGB Distance Field PNGとして格納する。

制限事項: 全入力は同一解像度、黒背景、色ごとに単一の連結領域を前提とする。領域の分裂・統合、穴、同色の複数島は対象外。PSD.jsが対応しない色深度・カラーモードのPSDはPNGへ変換して入力する。
